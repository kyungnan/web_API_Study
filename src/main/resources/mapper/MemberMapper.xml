<!-- 다른 mapper와 중복되지 않도록 네임스페이스 기재 -->
<mapper xmlns="http://mybatis.org/schema/mybatis-mapper" namespace="com.hellospring.demo.mapper.MemberMapper">
    <select id="getById" resultType="com.hellospring.demo.dto.MemberDto">
        select * from member where id = #{id};
    </select>
</mapper>